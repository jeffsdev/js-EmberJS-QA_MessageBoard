import { questionUnanswered } from 'qa-board/helpers/question-unanswered';
import { module, test } from 'qunit';

module('Unit | Helper | question unanswered');

// Replace this with your real tests.
test('it works', function(assert) {
  let result = questionUnanswered([42]);
  assert.ok(result);
});
